## Future steps

- fix the GA -> many functionalities to be added to the GA
- Validation Script: Separate script for out-of-distribution evaluation
- inference script -> inference for custom inputs
- logger script -> logs to an aws lambda functions that saves performance (to be done in the future) in a dynamo db (not for now, don't implement it until i tell you)
- demo & class scripts helper for running


-> validation and inference done but not tested yet!
-> window should not be computed by inputs, should be fixed by a yaml param instead -> fix this!